info:
  name: tes empty input
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/incidents
  body:
    type: json
    data: |-
      {
        "title": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
